<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vivo"
        tools:targetApi="31">

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Vivo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".AddFacultyActivity" />
        <activity android:name=".AddStudentActivity" />
        <activity android:name=".AddClassActivity" />
        <activity android:name=".AddCourseActivity" />
        <activity android:name=".FacultyDashboardActivity" />
        <activity android:name=".FacultyAttendanceActivity" />
        <activity android:name=".EditAttendanceActivity" />
        <activity android:name=".LowAttendanceActivity" />
        <activity android:name=".StudentDashboardActivity" />
        <activity android:name=".StudentAttendanceActivity" />
        <activity android:name=".AttendanceRecord" />
    </application>

</manifest>
